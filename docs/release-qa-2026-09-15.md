# FPC 디자인·접근 관리 배포 QA — 2026-09-15

## 방송 연동 계약

기준 운영 버전: `e5c17e86b8cd0386b2f93092a32b6a5bc79e1e8c`.

- `/api/v1` 및 경기 상태·공격 위치·xG·운영권·방송 상태를 포함한 **26개 경로, 참조 스키마 10개**의 OpenAPI 정의가 동일하다.
- 운영 DB 복사본의 경기 목록 및 종료 경기 3개에서 **22개 응답**을 비교했다. 동적으로 생성되는 시간 필드만 제외하고 값과 구조가 동일하다.
- FLA 핵심 핸들러/헬퍼 25개는 AST가 동일하며, `services.py`, `xg.py`, `xg_cone.py`는 파일 내용이 동일하다.
- 브랜딩 출처 정보는 내부에만 저장한다. 공개 방송 상태/스냅샷/경기 목록에는 필드를 추가하지 않는다.
- 계정 로그인과 PDF·팀 로고 관리 변경은 이번 기능 범위다. 기존 방송사용 인증 헤더, 이벤트 필드, 웹훅 형식은 유지한다.

## 검증 결과

| 검증 | 결과 |
| --- | --- |
| 인증·권한·계정 목록·세션 폐기 | 단위 테스트 7개 통과 |
| KFA PDF·유니폼·팀 로고 | 단위 테스트 13개 통과 |
| 골키퍼 xG·기존 카드 값 동기화 | 단위 테스트 7개 통과 |
| Linux 전체 API + PostgreSQL 통합 검사 | 8개 통과 |
| 웹 Linux Docker 빌드 | Next.js 빌드·타입 검사 통과 |
| 배포 Workflow | YAML 파싱 및 Bash 구문 검사 통과 |

통합 검사는 운영 DB 백업을 별도 PostgreSQL 컨테이너에 복원해 실행했다. QA 네트워크는 외부 통신을 차단했고, 알림·방송 에셋·웹훅 전송 워커를 테스트 중 중지했다. 운영 경기의 상태나 이벤트를 생성하지 않았다.

중복 상태/이벤트, 오래된 시간 입력, 명시적 시간 되감기, 점유 구간 합계, 관리자 운영권 인계, operator 코드 회전, 방송사 API 키, 종료 경기 쓰기 차단을 확인했다. 실제 제공 PDF의 홈/원정 각 20명(선발 11명)과 유니폼 색상을 확인했고, 팀 로고 업로드/자동 적용 및 **1920×1080 골키퍼 PNG** 생성까지 실행했다. 기존 `0.194` 기대실점이 모든 상대 슈팅 합계 `2.100`으로 보정되며 선택한 지표 5개를 유지한다.

데스크톱/모바일에서 사이드바, 밝은 테마, FPA 입력부, xG/xGOT 정렬, 공격 방향과 위치의 12개 조합을 확인했다. 배포 후 운영 도메인의 페이지·API·인증을 추가 확인한다.

## 배포 및 복구 준비

- 운영 경기 677개 모두 종료 상태임을 배포 전에 확인했다.
- 관리자 6명의 비공개 해시 파일을 서버 `runtime/auth/admin-accounts.json`에 권한 `600`으로 전달했다. 원본 비밀번호나 계정 해시는 Git/정적 웹/로그에 포함하지 않는다.
- 배포 Workflow가 새 API로 교체하기 전에 계정 파일 형식을 검증한다.
- 기존 `Deploy Production` 수동 Workflow로 배포한다.
- 이전 DB와 Compose 파일은 서버 `/home/ubuntu/deploy-backups/fpc-20260915/`에 보관한다.
- 이전 컨테이너 이미지는 `fpc-api-rollback:20260915`, `fpc-web-rollback:20260915`로 보존한다. 복구 시 저장한 Compose 설정과 해당 이미지를 사용하고 nginx를 다시 로드한다. 새 인증 테이블은 추가 방식이므로 이미지 복구 시 운영 DB를 과거 백업으로 덮어쓸 필요가 없다.
- 이번 인증 변경으로 기존 브라우저 세션은 다시 로그인해야 한다. Operator는 관리자가 System에서 코드를 설정해야 접근할 수 있다.

비공개 운영 응답 스냅샷과 DB 백업은 저장소에 넣지 않는다. 통합 테스트는 `FPC_RELEASE_QA=1`과 DB 이름 `fpc_release_qa`를 모두 요구하며, 일반 단위 테스트에서는 건너뛴다.
